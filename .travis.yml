sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t robertotesta/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true robertotesta/react-test npm test

after_success:
  - docker build -t robertotesta/multi-client ./client
  - docker build -t robertotesta/multi-nginx ./nginx
  - docker build -t robertotesta/multi-server ./server
  - docker build -t robertotesta/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push robertotesta/multi-client
  - docker push robertotesta/multi-nginx
  - docker push robertotesta/multi-server
  - docker push robertotesta/multi-worker
